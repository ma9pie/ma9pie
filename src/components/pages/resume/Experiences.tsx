import React from 'react';
import tw, { styled } from 'twin.macro';

import Container from '@/components/pages/resume/Container';

const Experiences = () => {
  return <Container>Experiences</Container>;
};

export default Experiences;
